[fixture]
rule_id = "FM002"
tier = 1
description = "Invalid YAML syntax in frontmatter"

[[variants]]
name = "unclosed-brace"
kind = "failing"
expected_count = 1
# AS001 is a structural consequence of FM002: when YAML is syntactically invalid
# the parser cannot extract fields, so the name appears missing in source.
allowed_collateral = ["AS001"]
