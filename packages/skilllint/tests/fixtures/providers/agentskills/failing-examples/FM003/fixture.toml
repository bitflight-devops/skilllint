[fixture]
rule_id = "FM003"
tier = 1
description = "No YAML frontmatter found — file lacks opening --- delimiter"

[[variants]]
name = "no-frontmatter"
kind = "failing"
expected_count = 1
# AS001 is collateral: when frontmatter is absent, the parser returns an empty
# dict, so name appears missing independently of FM003.
allowed_collateral = ["AS001"]
