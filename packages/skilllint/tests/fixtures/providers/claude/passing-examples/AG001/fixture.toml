[fixture]
rule_id = "AG001"
tier = 1
description = "Claude agent tools grant uses a documented server-scoped wildcard"

[[variants]]
name = "server-scoped-mcp-wildcard"
kind = "passing"
expected_count = 0
