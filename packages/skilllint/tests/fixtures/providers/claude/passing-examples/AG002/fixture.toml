[fixture]
rule_id = "AG002"
tier = 1
description = "Claude agent MCP tool grant matches the configured server-name casing"

[[variants]]
name = "exact-server-case"
kind = "passing"
expected_count = 0
