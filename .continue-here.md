---
phase: linting-cleanup
status: in_progress
last_updated: 2026-03-12T16:12:59.342Z
---

<current_state>
All prek linting issues resolved and pushed. Two known design fixes remain.
</current_state>

<completed_work>
- Installed GSD plugin + updated skilllint schemas (claude_code, codex, cursor)
- Added .pre-commit-config.yaml
- Fixed all prek --all-files failures (chmod, biome, ruff, ty)
- Extracted token_counter.py as first-class module (4400 warn / 8800 error thresholds)
- Wired AS005 to token thresholds, removed fabricated _MAX_BODY_LINES=500
- Ran skilllint against the-rewrite-room plugin — exit 0, info only
- All changes committed and pushed to origin/main
</completed_work>

<remaining_work>
1. Fix PL001: unset CLAUDECODE env var when PluginStructureValidator spawns
   `claude plugin validate` subprocess (nested CLI detection false positive)

2. Fix NR001: namespace derived from directory name is wrong — must read `"name"`
   field from plugin.json. Requires rewriting _find_plugins_root/_resolve_* methods
   and updating test fixtures to create plugin.json files.
   BUILTIN_AGENTS hardcoded list is also stale.
</remaining_work>

<decisions_made>
- Token thresholds empirically grounded from official Anthropic skill sizes (~4200 avg)
- token_counter.py is first-class, not a utility — all token counting flows through it
- NR001 directory-name assumption is a design bug, not a constraint to preserve
</decisions_made>

<next_action>
Fix PL001 first (small): add env={**os.environ, "CLAUDECODE": ""} to subprocess call
in PluginStructureValidator. Then tackle NR001 namespace-from-plugin.json refactor.
</next_action>
