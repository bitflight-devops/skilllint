---
phase: ci-release-setup
status: in_progress
last_updated: 2026-03-12T17:39:54.521Z
---

<current_state>
CI is failing on the `ty` typecheck job. `ty` was just added as a dev dependency and the workflow was restored to use `ty check packages/`. A new CI run is in progress (run ID 23015560776 was the basedpyright run — a newer run for the ty fix was just pushed). Need to verify the ty configuration matches what vm-flightsimulator uses, which is what the user asked to check before context ran out.
</current_state>

<completed_work>

- Fixed PL001: unset CLAUDECODE in subprocess env for both subprocess.run calls in PluginStructureValidator and validate_with_claude
- Fixed NR001: added _build_plugin_name_map() to NamespaceReferenceValidator — reads plugin.json "name" field instead of using directory name; updated all three _resolve_*() methods to accept plugin_dir directly
- Fixed test_third_party_adapter_discovery: stale mock patch target updated from `skilllint.adapters.importlib.metadata.entry_points` to `skilllint.adapters.registry.importlib.metadata.entry_points`
- All 559 tests passing locally (558 + 1 fixed)
- Committed and pushed all fixes: commit c9c63c7
- Created .github/workflows/auto-publish.yml (triggers on GitHub release published → uv build → uv publish via OIDC)
- Created .github/workflows/test.yml (format/lint/typecheck/test matrix + release job using mathieudutour/github-tag-action + ncipollo/release-action)
- Confirmed RELEASE_TOKEN is org-level secret scoped to ALL repos — already available, no action needed
- Added ty as dev dependency (uv add --dev ty → ty==0.0.21)
- Reverted workflow typecheck job back to `uv run ty check packages/`
</completed_work>

<remaining_work>

1. Verify ty configuration in vm-flightsimulator matches what we need here — user asked to check `/home/ubuntulinuxqa2/repos/vm-flightsimulator` ty config before context ran out
2. Monitor CI run for the latest push (fix: add ty as dev dependency) — confirm all jobs pass including typecheck (ty) and release job fires
3. Confirm PyPI publish succeeds after release is created
4. Set up PyPI trusted publishing if not already configured for skilllint package (go to PyPI project → Trusted Publishers → add bitflight-devops/agentskills-linter, workflow auto-publish.yml)
</remaining_work>

<decisions_made>

- RELEASE_TOKEN: org-level secret already covers this repo — no new token needed
- ty not basedpyright: user confirmed ty is the type checker for this project
- auto-publish.yml triggers on release published (not on push) — matches vm-flightsimulator pattern
- release job uses `default_bump: false` — only creates tag when commit message has conventional prefix (fix:, feat:, etc.)
</decisions_made>

<blockers>
- PAT creation API returned 404 — root cause unknown, but moot since org secret covers it
- ty was missing from dev deps — fixed (ty==0.0.21 added)
</blockers>

<context>
The CI pipeline is: push to main → test.yml runs format/lint/typecheck/tests → if all pass, release job auto-tags from conventional commits → GitHub release published → auto-publish.yml triggers → uv build + uv publish to PyPI.

The fix: commits from this session (PL001, NR001, test mock patch) will trigger a patch version bump when CI passes.

The user wants ty configuration to match vm-flightsimulator. Check: /home/ubuntulinuxqa2/repos/vm-flightsimulator/pyproject.toml [tool.ty] section and compare to this repo's pyproject.toml [tool.ty] section. The CI failure was "Failed to spawn: ty" — ty wasn't in dev deps. Now it is. But the ty config itself may also need adjusting.
</context>

<next_action>
1. /clear to free context
2. /gsd:resume-work
3. Read /home/ubuntulinuxqa2/repos/vm-flightsimulator/pyproject.toml [tool.ty] section
4. Compare to this repo's pyproject.toml [tool.ty] section
5. Update if needed, then monitor CI run
</next_action>
