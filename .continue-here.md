---
plan: docs/superpowers/plans/2026-03-11-vendor-drift-detection.md
task: ready to start Task 1.1
total_tasks: 8
status: in_progress
last_updated: 2026-03-11
---

<current_state>
Plan written and task list created. No implementation has started yet. All 8 tasks are pending.
Task IDs in TodoWrite: #12 (1.1), #13 (1.2), #14 (1.3), #15 (1.4), #16 (1.5), #17 (2.1+2.2), #18 (3.1+3.2)
</current_state>

<completed_work>

This session (pre-implementation):
- Ran fetch_platform_docs.py to pull cursor vendor docs (were missing)
- Audited all 3 schemas (cursor, codex, claude_code) against official vendor docs
- Fixed cursor schema: removed `description` from required, fixed globs type (string only, not string|array)
- Rewrote all 3 schemas with x-audited provenance metadata (date + source per field)
- Fixed claude_code schema: removed phantom fields, added missing documented fields, corrected required/optional
- Fixed codex schema: added host_executable entry
- Updated adapter.py files to read new `fields` object format (555 tests passing)
- Wrote audit reports to .claude/reports/
- Brainstormed and spec'd vendor drift detection feature
- Wrote implementation plan to docs/superpowers/plans/2026-03-11-vendor-drift-detection.md

</completed_work>

<remaining_work>

- Task 1.1: Add dataclasses (GitDriftResult, HttpFileDriftResult, HttpDriftResult, DriftReport), _sha256, _read_text_or_none to scripts/fetch_platform_docs.py. TDD.
- Task 1.2: Add _git_head_sha, modify clone_or_update_repo to return GitDriftResult. TDD.
- Task 1.3: Modify fetch_doc_site to return HttpDriftResult, add releases_url to DocSitePlatform, content hashing. Research Cursor and Copilot CLI changelog URLs. TDD.
- Task 1.4: _write_drift_report, modify fetch command to collect results, write .drift-pending.json, raise typer.Exit(code=2) on changes. TDD.
- Task 1.5: Quality gate — ruff, type check, all tests pass, coverage >= 80%.
- Task 2.1+2.2: Create .claude/hooks/vendor-drift-check.js (CommonJS, stdin/stdout JSON protocol, 60s timeout, runs uv run scripts/fetch_platform_docs.py, injects additionalContext on exit 2). Register in .claude/settings.json SessionStart array.
- Task 3.1+3.2: Create .claude/agents/schema-drift-auditor.md — detection-only agent, reads .drift-pending.json, cross-references x-audited.source fields, writes .drift-report.md. No Edit tool (read + write only).

</remaining_work>

<decisions_made>

- Drift detection: git vendors use SHA comparison, HTTP vendors use SHA-256 content hash
- .drift-pending.json written to .claude/vendor/ (gitignored by existing pattern)
- Exit code 2 = changes, 0 = no changes, dry-run always 0
- Releases/changelog page fetched for ALL vendors (not just git) — every platform has a releases page
- Drift report is detection only — no schema patching, no mechanical apply
- Agent does field-by-field assessment looking for: renames, new/removed fields, changed required status, deprecated terms
- Hook is silent on exit 0, injects additionalContext on exit 2
- Schema changes require human judgment + agent-driven audit (same process as this session's manual audit)

</decisions_made>

<blockers>
None.
</blockers>

<context>
The whole point of this feature: schemas have x-audited.source fields pointing to vendor docs. When those docs change, we need to know which schema fields might be stale. The drift auditor agent reads the actual diff + changelog and reports which fields need re-auditing — it does not auto-apply changes because schema correctness requires careful human+agent review (we got 13 fields wrong in the initial claude_code schema).
</context>

<next_action>
Start Task 1.1: dispatch python3-development:python-cli-architect subagent to add dataclasses and helpers to scripts/fetch_platform_docs.py with tests first. Plan is at docs/superpowers/plans/2026-03-11-vendor-drift-detection.md (Task 1.1 section).
</next_action>
