---
phase: 02-platform-adapters
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/skilllint/adapters/__init__.py
  - packages/skilllint/adapters/protocol.py
  - packages/skilllint/rules/__init__.py
  - packages/skilllint/rules/as_series.py
autonomous: true
requirements:
  - ADPT-01

must_haves:
  truths:
    - "PlatformAdapter Protocol is importable from skilllint.adapters.protocol"
    - "isinstance(adapter, PlatformAdapter) returns True for any object implementing the four protocol methods"
    - "AS-series rules AS001-AS006 fire correctly on SKILL.md content"
    - "AS-series runs once per file — not once per matching adapter"
  artifacts:
    - path: "packages/skilllint/adapters/protocol.py"
      provides: "PlatformAdapter @runtime_checkable Protocol"
      exports: ["PlatformAdapter"]
    - path: "packages/skilllint/adapters/__init__.py"
      provides: "load_adapters() registry function"
      exports: ["load_adapters"]
    - path: "packages/skilllint/rules/as_series.py"
      provides: "run_as_series() returning list of Violation for AS001-AS006"
      exports: ["run_as_series", "AS_RULES"]
  key_links:
    - from: "tests/test_adapters.py"
      to: "skilllint.adapters.protocol.PlatformAdapter"
      via: "import at test collection time"
    - from: "tests/test_as_series.py"
      to: "skilllint.rules.as_series.run_as_series"
      via: "import at test collection time"
    - from: "adapters/__init__.py"
      to: "importlib.metadata.entry_points"
      via: "load_adapters() calls entry_points(group='skilllint.adapters')"
---

<objective>
Define the PlatformAdapter Protocol and implement AS-series rules (AS001-AS006). This is the foundational layer — all three adapter implementations in plan 02-04 depend on the Protocol contract, and AS-series must be implemented before adapter tests can pass.

Purpose: Satisfies ADPT-01 and provides the cross-platform AS-series rule set that fires on any SKILL.md file regardless of which adapter is active.
Output: adapters/protocol.py, adapters/__init__.py, rules/as_series.py
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-adapters/02-RESEARCH.md
@.planning/phases/02-platform-adapters/02-CONTEXT.md

<interfaces>
<!-- Existing pattern from Phase 1 — executor uses this, do not re-derive -->
<!-- From packages/skilllint/__init__.py -->
```python
# load_bundled_schema() pattern to follow for schema access in adapters
from importlib.resources import files
import json

def load_bundled_schema(platform: str, version: str = "v1") -> dict:
    pkg = f"skilllint.schemas.{platform}"
    data = files(pkg).joinpath(f"{version}.json").read_text(encoding="utf-8")
    return json.loads(data)
```

<!-- Violation namedtuple/dataclass shape — check existing plugin_validator.py for the actual Violation type -->
<!-- The AS-series run_as_series() must return the same Violation type as the rest of the codebase -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlatformAdapter Protocol + load_adapters registry</name>
  <files>packages/skilllint/adapters/__init__.py, packages/skilllint/adapters/protocol.py</files>
  <behavior>
    - test_protocol_defined: from skilllint.adapters.protocol import PlatformAdapter succeeds
    - test_runtime_checkable: a minimal duck-typed class with platform_id, path_patterns(), applicable_rules(), get_schema() satisfies isinstance(obj, PlatformAdapter) == True
    - test_entry_points_discovery: load_adapters() returns a dict; after uv pip install -e ., all three bundled adapters appear by ID
    - test_third_party_adapter_discovery: mock a fourth entry_point in the "skilllint.adapters" group; load_adapters() returns it without core modification
  </behavior>
  <action>
    Create packages/skilllint/adapters/__init__.py and packages/skilllint/adapters/protocol.py.

    protocol.py: Define @runtime_checkable Protocol named PlatformAdapter with exactly these four methods:
    - platform_id: property returning str
    - path_patterns() -> list[str]
    - applicable_rules() -> set[str]
    - get_schema(file_type: str) -> dict | None

    Do NOT add matches_file() to the Protocol — keep the Protocol minimal. matches_file() can be a standalone utility function in __init__.py that takes an adapter and a PurePath.

    adapters/__init__.py: Implement load_adapters() using importlib.metadata.entry_points(group="skilllint.adapters"). Returns dict[str, PlatformAdapter]. Also export matches_file(adapter: PlatformAdapter, path: PurePath) -> bool using PurePath.match(). Do not use pkg_resources (deprecated).

    Third-party adapter test: use unittest.mock to patch importlib.metadata.entry_points to return a fake EntryPoint for the fourth adapter — do not require a real installed dist for the unit test. The VALIDATION.md manual-only test covers real installation.
  </action>
  <verify>
    <automated>cd packages/skilllint && uv run pytest tests/test_adapters.py::test_protocol_defined tests/test_adapters.py::test_runtime_checkable tests/test_adapters.py::test_entry_points_discovery tests/test_adapters.py::test_third_party_adapter_discovery -x -q</automated>
  </verify>
  <done>All four protocol/registry tests pass. isinstance(mock_adapter, PlatformAdapter) returns True without inheritance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AS-series rules AS001–AS006</name>
  <files>packages/skilllint/rules/__init__.py, packages/skilllint/rules/as_series.py</files>
  <behavior>
    - test_as001_name_format_valid: run_as_series on SKILL.md with name: "my-skill" returns no AS001
    - test_as001_name_format_invalid: name "My_Skill!" returns AS001 error
    - test_as001_name_too_long: name of 65 chars returns AS001 error
    - test_as001_consecutive_hyphens: name "my--skill" returns AS001 error
    - test_as002_name_matches_directory: name "foo" in dir path ending in "bar" returns AS002 error; name "bar" in dir "bar" returns no AS002
    - test_as003_description_present: missing description field returns AS003 error; empty string returns AS003 error
    - test_as004_description_no_html: description "<b>text</b>" returns AS004 error; plain text returns no AS004
    - test_as005_body_length_warning: body with 501 lines returns AS005 warning; body with 499 lines returns no AS005
    - test_as006_no_eval_queries_info: when no eval_queries.json exists in same directory, returns AS006 info; when it exists, no AS006
  </behavior>
  <action>
    Create packages/skilllint/rules/__init__.py (empty, makes it a package) and packages/skilllint/rules/as_series.py.

    First: read packages/skilllint/plugin_validator.py to find the existing Violation namedtuple or dataclass definition. AS-series must return the same Violation type — do not define a new one. Import it from skilllint.plugin_validator or factor it to a shared location if it doesn't exist as a standalone import.

    run_as_series(path: Path, frontmatter: dict, body_lines: list[str]) -> list[Violation]:

    AS001: name must match ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$ AND len 1–64 AND no consecutive hyphens. Severity: error.
    AS002: name value must equal path.parent.name (parent directory name). Severity: error.
    AS003: description field must be present, non-empty after strip. Severity: error.
    AS004: description must not contain < or >. Severity: error.
    AS005: len(body_lines) > 500 produces warning. Severity: warning.
    AS006: path.parent does not contain eval_queries.json or any file matching *eval*.json or *queries*.json. Severity: info.

    Export AS_RULES: dict mapping code -> description string, for use by other subsystems (LSP hover, list_rules MCP tool in future phases).

    run_as_series is a pure function — no side effects, no file I/O beyond the path.parent check for AS006.
  </action>
  <verify>
    <automated>cd packages/skilllint && uv run pytest tests/test_as_series.py -x -q</automated>
  </verify>
  <done>All AS-series tests pass. run_as_series returns the same Violation type as the rest of the codebase.</done>
</task>

</tasks>

<verification>
`cd packages/skilllint && uv run pytest tests/test_adapters.py::test_protocol_defined tests/test_adapters.py::test_runtime_checkable tests/test_adapters.py::test_entry_points_discovery tests/test_adapters.py::test_third_party_adapter_discovery tests/test_as_series.py -q` — all pass.

Pre-existing suite not broken: `cd packages/skilllint && uv run pytest -x -q --ignore=tests/test_adapters.py --ignore=tests/test_as_series.py`
</verification>

<success_criteria>
- PlatformAdapter Protocol importable, @runtime_checkable, four methods defined
- load_adapters() returns dict[str, PlatformAdapter] from entry_points
- AS001–AS006 return correct Violation objects with matching type to existing codebase
- All protocol + AS-series tests GREEN
- Pre-existing 529 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-adapters/02-02-SUMMARY.md`
</output>
