---
phase: 02-platform-adapters
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/skilllint/tests/test_adapters.py
  - packages/skilllint/tests/test_as_series.py
  - packages/skilllint/tests/fixtures/claude_code/valid_plugin.json
  - packages/skilllint/tests/fixtures/claude_code/invalid_plugin.json
  - packages/skilllint/tests/fixtures/claude_code/valid_skill.md
  - packages/skilllint/tests/fixtures/claude_code/invalid_skill.md
  - packages/skilllint/tests/fixtures/cursor/valid_rule.mdc
  - packages/skilllint/tests/fixtures/cursor/invalid_rule.mdc
  - packages/skilllint/tests/fixtures/cursor/valid_skill.md
  - packages/skilllint/tests/fixtures/codex/valid_agents.md
  - packages/skilllint/tests/fixtures/codex/empty_agents.md
  - packages/skilllint/tests/fixtures/codex/valid_rules.rules
  - packages/skilllint/tests/fixtures/codex/invalid_rules.rules
  - packages/skilllint/tests/fixtures/codex/valid_skill.md
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03
  - ADPT-04
  - ADPT-05

must_haves:
  truths:
    - "Test files exist and all tests fail (RED) — implementations do not exist yet"
    - "Fixture files contain realistic valid and invalid examples for each platform"
    - "Test names match VALIDATION.md task IDs exactly for traceability"
  artifacts:
    - path: "packages/skilllint/tests/test_adapters.py"
      provides: "Test stubs for ADPT-01 through ADPT-05"
      exports: ["test_protocol_defined", "test_runtime_checkable", "test_entry_points_discovery", "test_third_party_adapter_discovery", "test_claude_code_path_patterns", "test_cursor_adapter_mdc_validation", "test_codex_agents_md_validation", "test_codex_rules_field_validation"]
    - path: "packages/skilllint/tests/test_as_series.py"
      provides: "Test stubs for AS001 through AS006"
      exports: ["test_as001_name_format", "test_as002_name_matches_directory", "test_as003_description_present", "test_as004_description_no_html", "test_as005_body_length_warning", "test_as006_no_eval_queries_info"]
    - path: "packages/skilllint/tests/fixtures/claude_code/valid_plugin.json"
      provides: "Minimal valid plugin.json for Claude Code"
    - path: "packages/skilllint/tests/fixtures/cursor/valid_rule.mdc"
      provides: "Valid .mdc file with description, globs, alwaysApply"
    - path: "packages/skilllint/tests/fixtures/codex/valid_rules.rules"
      provides: "Valid Starlark .rules file with prefix_rule() call"
  key_links:
    - from: "tests/test_adapters.py"
      to: "skilllint.adapters.protocol.PlatformAdapter"
      via: "import — fails RED until plan 02-02 creates the module"
    - from: "tests/test_as_series.py"
      to: "skilllint.rules.as_series"
      via: "import — fails RED until plan 02-02 creates the module"
---

<objective>
Create failing test stubs and fixture files for all Phase 2 adapter requirements before any implementation exists. This is the Wave 0 TDD scaffold — tests must fail RED, then turn GREEN as implementations land in subsequent plans.

Purpose: Nyquist compliance — no task in plans 02-02 through 02-05 runs without an automated verify command. All test file references in VALIDATION.md are created here.
Output: test_adapters.py, test_as_series.py, and five fixture directories with valid/invalid examples.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-adapters/02-CONTEXT.md
@.planning/phases/02-platform-adapters/02-RESEARCH.md
@.planning/phases/02-platform-adapters/02-VALIDATION.md
@.planning/ROADMAP.md
</context>

<feature>
  <name>Wave 0: Test scaffolds and fixtures</name>
  <files>
    packages/skilllint/tests/test_adapters.py,
    packages/skilllint/tests/test_as_series.py,
    packages/skilllint/tests/fixtures/claude_code/valid_plugin.json,
    packages/skilllint/tests/fixtures/claude_code/invalid_plugin.json,
    packages/skilllint/tests/fixtures/claude_code/valid_skill.md,
    packages/skilllint/tests/fixtures/claude_code/invalid_skill.md,
    packages/skilllint/tests/fixtures/cursor/valid_rule.mdc,
    packages/skilllint/tests/fixtures/cursor/invalid_rule.mdc,
    packages/skilllint/tests/fixtures/cursor/valid_skill.md,
    packages/skilllint/tests/fixtures/codex/valid_agents.md,
    packages/skilllint/tests/fixtures/codex/empty_agents.md,
    packages/skilllint/tests/fixtures/codex/valid_rules.rules,
    packages/skilllint/tests/fixtures/codex/invalid_rules.rules,
    packages/skilllint/tests/fixtures/codex/valid_skill.md
  </files>
  <behavior>
    test_adapters.py RED behaviors:
    - test_protocol_defined: imports PlatformAdapter from skilllint.adapters.protocol — ImportError (module missing)
    - test_runtime_checkable: isinstance check on a mock adapter — ImportError
    - test_entry_points_discovery: calls load_adapters() from skilllint.adapters — ImportError
    - test_third_party_adapter_discovery: mocks a fourth entry_point, verifies load_adapters() returns it — ImportError
    - test_claude_code_path_patterns: asserts ClaudeCodeAdapter().path_patterns() contains ".claude/**/*.md" — ImportError
    - test_cursor_adapter_mdc_validation: validates valid_rule.mdc with no violations — ImportError
    - test_cursor_mdc_unknown_fields: expects violation for unknown field "unknown_field: true" in .mdc — ImportError
    - test_codex_agents_md_validation: empty AGENTS.md produces violation; non-empty passes — ImportError
    - test_codex_rules_field_validation: unknown prefix_rule field "owner" produces violation — ImportError
    - test_codex_path_patterns: asserts CodexAdapter().path_patterns() contains "AGENTS.md" — ImportError

    test_as_series.py RED behaviors:
    - test_as001_name_format_valid: name "my-skill" passes (no AS001)
    - test_as001_name_format_invalid: name "My_Skill!" produces AS001 error
    - test_as002_name_matches_directory: name "foo" in dir "bar/" produces AS002 error
    - test_as003_description_present: missing description produces AS003 error
    - test_as004_description_no_html: description containing "<b>" produces AS004 error
    - test_as005_body_length_warning: SKILL.md body with 501 lines produces AS005 warning
    - test_as006_no_eval_queries_info: SKILL.md directory with no eval_queries.json produces AS006 info

    All test_as_series.py tests: ImportError until plan 02-02 creates skilllint.rules.as_series
  </behavior>
  <implementation>
    Write test stubs that import from modules that do not yet exist. Use pytest.raises(ImportError) where appropriate, OR structure tests so they naturally fail ImportError. The cleaner approach: write tests that directly import the not-yet-existing modules at the top of each test file — the import failure makes the entire file fail collection with ImportError, but each individual test function body should contain real assertions using fixtures so tests turn GREEN when modules land.

    Fixtures: create fixture files with realistic content matching the schema definitions in RESEARCH.md.

    valid_plugin.json: minimal valid Claude Code plugin.json (name, description, version, schema_version)
    invalid_plugin.json: missing required "name" field
    valid_skill.md (claude_code): YAML frontmatter with valid name/description per agentskills.io spec, body under 500 lines
    invalid_skill.md (claude_code): frontmatter with name "My_Skill!" (fails AS001)
    valid_rule.mdc: frontmatter with description: "Enforce X", globs: ["*.ts"], alwaysApply: false
    invalid_rule.mdc: frontmatter with unknown_field: true (no description — fails Cursor schema)
    valid_skill.md (cursor): valid agentskills.io SKILL.md for cursor fixtures
    valid_agents.md (codex): non-empty AGENTS.md with content
    empty_agents.md (codex): completely empty file
    valid_rules.rules: single prefix_rule() call with pattern, decision, justification fields
    invalid_rules.rules: prefix_rule() call with unknown field "owner"
    valid_skill.md (codex): valid agentskills.io SKILL.md under .agents/skills/my-skill/

    Run after: cd packages/skilllint && uv run pytest tests/test_adapters.py tests/test_as_series.py --collect-only
    Expected: collection errors (ImportError) — this is the correct RED state.
  </implementation>
</feature>

<verification>
RED state confirmed: `cd packages/skilllint && uv run pytest tests/test_adapters.py tests/test_as_series.py --collect-only 2>&1 | grep -c "ImportError\|ModuleNotFoundError"` returns a count > 0.

Fixture files exist: `ls packages/skilllint/tests/fixtures/claude_code/ packages/skilllint/tests/fixtures/cursor/ packages/skilllint/tests/fixtures/codex/`
</verification>

<success_criteria>
- test_adapters.py and test_as_series.py exist with all stubs listed in the behavior section
- All fixture directories exist with at least the files listed in files_modified
- Running `--collect-only` confirms ImportError from missing skilllint.adapters.protocol and skilllint.rules.as_series
- No test passes yet (all RED or collection error)
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-adapters/02-01-SUMMARY.md`
</output>
