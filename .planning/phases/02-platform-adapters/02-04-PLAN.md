---
phase: 02-platform-adapters
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - packages/skilllint/adapters/claude_code.py
  - packages/skilllint/adapters/cursor.py
  - packages/skilllint/adapters/codex.py
  - packages/skilllint/pyproject.toml
autonomous: true
requirements:
  - ADPT-02
  - ADPT-03
  - ADPT-04
  - ADPT-05

must_haves:
  truths:
    - "ClaudeCodeAdapter satisfies isinstance(a, PlatformAdapter) and returns SK/PR/HK/AS in applicable_rules()"
    - "CursorAdapter satisfies isinstance(a, PlatformAdapter) and validates .mdc frontmatter fields, rejecting unknown fields"
    - "CodexAdapter satisfies isinstance(a, PlatformAdapter) and validates AGENTS.md non-empty, .rules prefix_rule() field names"
    - "load_adapters() returns all three adapters by ID after uv pip install -e ."
    - "A SKILL.md file matching both Cursor and Codex path patterns gets AS-series fired once, not twice"
  artifacts:
    - path: "packages/skilllint/adapters/claude_code.py"
      provides: "ClaudeCodeAdapter with platform_id='claude_code'"
      exports: ["ClaudeCodeAdapter"]
    - path: "packages/skilllint/adapters/cursor.py"
      provides: "CursorAdapter with platform_id='cursor'"
      exports: ["CursorAdapter"]
    - path: "packages/skilllint/adapters/codex.py"
      provides: "CodexAdapter with platform_id='codex'"
      exports: ["CodexAdapter"]
  key_links:
    - from: "pyproject.toml [project.entry-points.'skilllint.adapters']"
      to: "skilllint.adapters.claude_code:ClaudeCodeAdapter"
      via: "entry_points registration — enables load_adapters() discovery"
    - from: "adapters/cursor.py CursorAdapter.get_schema('mdc')"
      to: "skilllint.schemas.cursor v1.json file_types.mdc"
      via: "load_bundled_schema('cursor', 'v1')['file_types']['mdc']"
    - from: "adapters/codex.py CodexAdapter.get_schema('prefix_rule')"
      to: "skilllint.schemas.codex v1.json file_types.prefix_rule"
      via: "load_bundled_schema('codex', 'v1')['file_types']['prefix_rule']"
---

<objective>
Implement the three bundled adapter classes (ClaudeCodeAdapter, CursorAdapter, CodexAdapter) and register them in pyproject.toml under [project.entry-points."skilllint.adapters"]. Each adapter is a data provider only — no validation logic lives here.

Purpose: Satisfies ADPT-02, ADPT-03, ADPT-04, ADPT-05. After this plan, `load_adapters()` returns all three adapters and the test suite for per-platform validation passes.
Output: claude_code.py, cursor.py, codex.py adapters + pyproject.toml entry_points registration
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-adapters/02-RESEARCH.md
@.planning/phases/02-platform-adapters/02-02-SUMMARY.md
@.planning/phases/02-platform-adapters/02-03-SUMMARY.md

<interfaces>
<!-- PlatformAdapter Protocol (from plan 02-02) -->
```python
# packages/skilllint/adapters/protocol.py
from typing import Protocol, runtime_checkable

@runtime_checkable
class PlatformAdapter(Protocol):
    @property
    def platform_id(self) -> str: ...
    def path_patterns(self) -> list[str]: ...
    def applicable_rules(self) -> set[str]: ...
    def get_schema(self, file_type: str) -> dict | None: ...
```

<!-- load_adapters() (from plan 02-02) -->
```python
# packages/skilllint/adapters/__init__.py
from importlib.metadata import entry_points
def load_adapters() -> dict[str, PlatformAdapter]: ...
```

<!-- AS-series (from plan 02-02) -->
```python
# packages/skilllint/rules/as_series.py
def run_as_series(path: Path, frontmatter: dict, body_lines: list[str]) -> list[Violation]: ...
```

<!-- Path patterns per RESEARCH.md loading location matrix -->
ClaudeCodeAdapter.path_patterns():
  [".claude/**/*.md", "plugin.json", "hooks.json", "agents/**/*.md", "commands/**/*.md"]

CursorAdapter.path_patterns():
  ["**/*.mdc", ".cursor/skills/**/*.md", ".claude/skills/**/*.md", ".agents/skills/**/*.md"]
  Note: NOT ".cursor/**/*.md" (too broad — see Pitfall 3 in RESEARCH.md)

CodexAdapter.path_patterns():
  [".agents/skills/**/*.md", "AGENTS.md", "**/*.rules", ".codex/**"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClaudeCodeAdapter</name>
  <files>packages/skilllint/adapters/claude_code.py</files>
  <behavior>
    - test_protocol_conformance: isinstance(ClaudeCodeAdapter(), PlatformAdapter) is True
    - test_claude_code_path_patterns: path_patterns() contains all five patterns from RESEARCH.md matrix
    - test_claude_code_applicable_rules: applicable_rules() returns set containing 'SK', 'PR', 'HK', 'AS'
    - test_claude_code_adapter_validates_all_file_types: validate a plugin.json, SKILL.md, agents/foo.md, hooks.json — no false positives on valid fixtures
  </behavior>
  <action>
    Create packages/skilllint/adapters/claude_code.py.

    ClaudeCodeAdapter class (no inheritance, satisfies PlatformAdapter Protocol structurally):
    - platform_id property: returns "claude_code"
    - path_patterns(): returns [".claude/**/*.md", "plugin.json", "hooks.json", "agents/**/*.md", "commands/**/*.md"]
    - applicable_rules(): returns {"SK", "PR", "HK", "AS"}
    - get_schema(file_type: str) -> dict | None: loads from load_bundled_schema("claude_code", "v1"). Map file_type strings: "plugin_json" -> top-level schema, "skill_md" -> skill_md sub-schema if present. Return None for unrecognized file_type.

    Adapters are DATA PROVIDERS only — no validation logic in the adapter class. The adapter returns schema data; the core validator (plan 02-05) runs the checks.

    Lazy schema loading: call load_bundled_schema() inside get_schema(), not at __init__ time.
  </action>
  <verify>
    <automated>cd packages/skilllint && uv run pytest tests/test_adapters.py::test_protocol_conformance tests/test_adapters.py::test_claude_code_path_patterns -x -q</automated>
  </verify>
  <done>ClaudeCodeAdapter passes isinstance check and returns correct path_patterns and applicable_rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CursorAdapter + CodexAdapter + entry_points registration</name>
  <files>packages/skilllint/adapters/cursor.py, packages/skilllint/adapters/codex.py, packages/skilllint/pyproject.toml</files>
  <behavior>
    CursorAdapter:
    - test_cursor_adapter_mdc_validation: CursorAdapter().get_schema('mdc') returns dict with required=['description'] and additionalProperties=false
    - test_cursor_mdc_unknown_fields: validating invalid_rule.mdc (has unknown field) produces a violation
    - test_cursor_skill_md_uses_as_series: CursorAdapter().applicable_rules() contains 'AS'
    - test_cursor_path_patterns: path_patterns() contains "**/*.mdc" and ".cursor/skills/**/*.md" but NOT ".cursor/**/*.md"

    CodexAdapter:
    - test_codex_agents_md_validation: empty AGENTS.md produces violation; non-empty passes
    - test_codex_rules_field_validation: .rules with unknown field "owner" produces violation; valid .rules passes
    - test_codex_path_patterns: path_patterns() contains "AGENTS.md", "**/*.rules", ".agents/skills/**/*.md"

    Entry points:
    - test_entry_points_discovery: after uv pip install -e ., load_adapters() keys include 'claude_code', 'cursor', 'codex'
  </behavior>
  <action>
    Create packages/skilllint/adapters/cursor.py:
    - platform_id: "cursor"
    - path_patterns(): ["**/*.mdc", ".cursor/skills/**/*.md", ".claude/skills/**/*.md", ".agents/skills/**/*.md"] — NOT ".cursor/**/*.md" (Pitfall 3)
    - applicable_rules(): {"AS"} — Cursor-specific rule series TBD; for Phase 2, only AS-series fires
    - get_schema("mdc") -> load_bundled_schema("cursor", "v1")["file_types"]["mdc"]
    - get_schema("skill_md") -> load_bundled_schema("cursor", "v1")["file_types"]["skill_md"]

    Create packages/skilllint/adapters/codex.py:
    - platform_id: "codex"
    - path_patterns(): [".agents/skills/**/*.md", "AGENTS.md", "**/*.rules", ".codex/**"]
    - applicable_rules(): {"AS"} — Codex-specific rule series TBD; for Phase 2, only AS-series fires
    - get_schema("prefix_rule") -> load_bundled_schema("codex", "v1")["file_types"]["prefix_rule"]
    - get_schema("agents_md") -> load_bundled_schema("codex", "v1")["file_types"]["agents_md"]
    - get_schema("skill_md") -> load_bundled_schema("codex", "v1")["file_types"]["skill_md"]

    Also implement two validation helper methods on CodexAdapter (not Protocol methods — these are concrete implementation details used by the adapter's run_platform_checks logic in plan 02-05):
    - validate_agents_md(content: str) -> list[str]: returns ["AGENTS.md is empty"] if content.strip() is falsy
    - validate_rules_file(content: str) -> list[str]: uses RESEARCH.md regex pattern to find prefix_rule() calls, extract field names, return violations for unknown fields and invalid decision values

    Read pyproject.toml to find the existing [project.entry-points] section. Add the three adapter entry points under [project.entry-points."skilllint.adapters"]:
      claude_code = "skilllint.adapters.claude_code:ClaudeCodeAdapter"
      cursor      = "skilllint.adapters.cursor:CursorAdapter"
      codex       = "skilllint.adapters.codex:CodexAdapter"

    After editing pyproject.toml, run: cd packages/skilllint && uv pip install -e . (to register the new entry points so test_entry_points_discovery passes).
  </action>
  <verify>
    <automated>cd packages/skilllint && uv run pytest tests/test_adapters.py -x -q</automated>
  </verify>
  <done>All tests in test_adapters.py pass. load_adapters() returns all three adapters. Cursor and Codex adapters validate their respective file types correctly.</done>
</task>

</tasks>

<verification>
Full adapter test suite green: `cd packages/skilllint && uv run pytest tests/test_adapters.py tests/test_as_series.py -q`

Pre-existing suite not broken: `cd packages/skilllint && uv run pytest -q`
</verification>

<success_criteria>
- All three adapters satisfy isinstance(adapter, PlatformAdapter) == True
- load_adapters() returns claude_code, cursor, codex keys
- Cursor adapter rejects .mdc with unknown fields; validates description as required
- Codex adapter rejects empty AGENTS.md; rejects .rules with unknown prefix_rule fields
- Full test suite (529 + new adapter tests) all GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-adapters/02-04-SUMMARY.md`
</output>
