---
phase: 01-package-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - packages/skilllint/__init__.py
  - packages/skilllint/version.py
  - packages/skilllint/py.typed
  - packages/skilllint/plugin_validator.py
  - packages/skilllint/frontmatter_core.py
  - packages/skilllint/frontmatter_utils.py
  - packages/skilllint/auto_sync_manifests.py
  - packages/skilllint/tests/conftest.py
  - uv.lock
  - README.md
  - LICENSE
autonomous: true
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "uv run pytest passes all 521 tests on main branch after merge"
    - "pyproject.toml exists in repo root with hatchling build backend and hatch-vcs versioning"
    - "packages/skilllint/__init__.py, version.py, and py.typed exist"
    - "uv build produces a .whl file in dist/"
    - "No PEP 723 shebang exists on line 1 of plugin_validator.py"
  artifacts:
    - path: "pyproject.toml"
      provides: "Hatchling build config with project metadata, dependencies, and wheel source mapping"
      contains: "[build-system]"
    - path: "packages/skilllint/__init__.py"
      provides: "Package init importing __version__"
    - path: "packages/skilllint/version.py"
      provides: "VCS-derived version string via hatch-vcs"
    - path: "packages/skilllint/py.typed"
      provides: "PEP 561 type marker"
    - path: "dist/skilllint-*.whl"
      provides: "Installable wheel artifact"
  key_links:
    - from: "pyproject.toml"
      to: "packages/skilllint"
      via: "[tool.hatch.build.targets.wheel.sources]"
      pattern: "packages/skilllint.*skilllint"
    - from: "packages/skilllint/__init__.py"
      to: "packages/skilllint/version.py"
      via: "from skilllint.version import __version__"
      pattern: "__version__"
---

<objective>
Merge the feature/initial-packaging worktree into main, establishing the hatchling-based installable package that replaces the PEP 723 script workflow.

Purpose: Everything in subsequent Phase 1 plans depends on the package structure being present in main. The feature/initial-packaging worktree already has 521/522 tests passing and a working .whl — this plan lands that work on main.
Output: main branch has pyproject.toml with hatchling backend, package markers (__init__.py, version.py, py.typed), passing test suite, and a buildable .whl.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-structure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge feature/initial-packaging into main</name>
  <files>
    pyproject.toml
    packages/skilllint/__init__.py
    packages/skilllint/version.py
    packages/skilllint/py.typed
    packages/skilllint/plugin_validator.py
    packages/skilllint/frontmatter_core.py
    packages/skilllint/frontmatter_utils.py
    packages/skilllint/auto_sync_manifests.py
    packages/skilllint/tests/conftest.py
    uv.lock
    README.md
    LICENSE
  </files>
  <action>
    Merge the feature/initial-packaging branch into main. The worktree is at .worktrees/initial-packaging with 7 commits ahead of main.

    Steps:
    1. Ensure working tree on main is clean: `git status`
    2. Merge: `git merge feature/initial-packaging --no-ff -m "feat(package): merge initial packaging — hatchling build, 4 CLI entry points, 521 tests passing"`
    3. If merge conflicts occur (likely in pyproject.toml which exists on both branches), resolve by taking the worktree version as the base and manually restoring any .planning/-related content from main. The worktree pyproject.toml is the canonical version.
    4. After merge, run `uv sync` to install the package in dev mode.
    5. Verify plugin_validator.py line 1 does NOT start with `#!` (the PEP 723 shebang was removed in commit 9e0cbf1 — confirm it is absent post-merge).
    6. Remove the dead `sys.path.insert(0, _SCRIPTS_DIR)` block from plugin_validator.py if present (lines ~67-69 that add the scripts dir to sys.path — no longer needed in package layout). Only remove if the block exists; if already removed in the worktree, skip.
    7. Run `uv build` to verify a .whl is produced in dist/.

    Do NOT modify any .planning/ files in this task — they are managed separately.
  </action>
  <verify>
    <automated>cd /home/ubuntulinuxqa2/repos/agentskills-linter && git log --oneline -5 && python -c "import skilllint; print(skilllint.__version__)" && uv build --quiet && ls dist/skilllint-*.whl</automated>
  </verify>
  <done>
    - main branch contains all 7 commits from feature/initial-packaging
    - `import skilllint` succeeds and prints a version string
    - `uv build` produces a .whl in dist/
    - plugin_validator.py line 1 is NOT a shebang
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify full test suite passes on merged main</name>
  <files>packages/skilllint/tests/conftest.py</files>
  <behavior>
    - All 521 previously-passing tests continue to pass (1 skip is acceptable)
    - No import errors from package restructure
    - Coverage threshold (60%) is met
  </behavior>
  <action>
    Run the full test suite on the merged main branch:
    1. `uv run pytest -x --tb=short` — stop at first failure to diagnose quickly
    2. If tests fail due to import errors (e.g., `ModuleNotFoundError: No module named 'frontmatter_core'`), the cause is conftest.py still using `spec_from_file_location` but the module name collision — check if `sys.path` manipulation was removed. The fix is to add `packages/` to pythonpath (already in pyproject.toml `[tool.pytest.ini_options] pythonpath = [".", "packages/"]`) and ensure `uv sync` was run.
    3. If mocker patches fail with `AttributeError` after any conftest.py changes, update patch targets to use full module path `skilllint.plugin_validator.X` instead of `plugin_validator.X`.
    4. Record the final pass/fail/skip count.

    Note: conftest.py currently uses `spec_from_file_location` — this still WORKS post-merge because `pythonpath` includes `packages/`. Do NOT change the conftest.py import method in this task (that is Plan 03's job). Only fix genuine failures.
  </action>
  <verify>
    <automated>cd /home/ubuntulinuxqa2/repos/agentskills-linter && uv run pytest --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    521+ tests pass (1 skip allowed). Zero failures. Coverage at or above 60%.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `git log --oneline -8` shows feature/initial-packaging commits present in main
2. `python -c "import skilllint; print(skilllint.__version__)"` prints a version string
3. `uv build` exits 0 and dist/ contains a .whl
4. `uv run pytest -q` exits 0 with 521+ passing
5. `head -1 packages/skilllint/plugin_validator.py` does NOT print a shebang line
</verification>

<success_criteria>
- main branch contains the full hatchling package structure from feature/initial-packaging
- Package is importable: `import skilllint` resolves
- Wheel is buildable: `uv build` produces .whl
- 521 tests pass on main
- No PEP 723 shebang in plugin_validator.py
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-structure/01-01-SUMMARY.md` with:
- What was merged (list of commits, key files added)
- Final test count (pass/skip/fail)
- Wheel file name produced
- Any merge conflicts encountered and how resolved
- Current pyproject.toml entry points (to inform Plan 02)
</output>
