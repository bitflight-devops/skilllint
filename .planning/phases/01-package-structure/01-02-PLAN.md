---
phase: 01-package-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pyproject.toml
  - packages/skilllint/schemas/__init__.py
  - packages/skilllint/schemas/claude_code/__init__.py
  - packages/skilllint/schemas/claude_code/v1.json
autonomous: true
requirements:
  - PKG-03
  - PKG-04

must_haves:
  truths:
    - "pluginlint --help executes without error after uv tool install dist/skilllint-*.whl"
    - "skilllint, agentlint, pluginlint, and skillint all produce identical --help output"
    - "uv build produces a .whl where unzip -l shows skilllint/schemas/claude_code/v1.json"
    - "importlib.resources.files('skilllint.schemas.claude_code').joinpath('v1.json').read_bytes() succeeds in a Python interpreter"
    - "Tests in wheel are excluded — unzip -l dist/*.whl shows no skilllint/tests/ entries"
  artifacts:
    - path: "pyproject.toml"
      provides: "All 4 CLI entry points + wheel test exclusion"
      contains: "pluginlint"
    - path: "packages/skilllint/schemas/claude_code/v1.json"
      provides: "Bundled Claude Code schema snapshot v1"
      contains: "$schema"
    - path: "packages/skilllint/schemas/__init__.py"
      provides: "Namespace package marker for importlib.resources traversal"
    - path: "packages/skilllint/schemas/claude_code/__init__.py"
      provides: "Namespace package marker for importlib.resources traversal"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "skilllint.plugin_validator:app"
      via: "all four aliases point to same function"
      pattern: "pluginlint.*plugin_validator"
    - from: "importlib.resources.files('skilllint.schemas.claude_code')"
      to: "packages/skilllint/schemas/claude_code/v1.json"
      via: "hatchling wheel include of packages/skilllint directory"
      pattern: "v1\\.json"
---

<objective>
Add the missing pluginlint CLI entry point, exclude tests from the distributed wheel, and create the bundled schema directory structure that Phase 2 adapters will populate.

Purpose: PKG-03 requires all four CLI aliases to work. PKG-04 requires schema JSON files accessible via importlib.resources.files() at runtime. Both gaps exist in the merged package.
Output: pyproject.toml with all 4 entry points and wheel test exclusion; packages/skilllint/schemas/claude_code/v1.json bundled in wheel and accessible via importlib.resources.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-package-structure/01-RESEARCH.md
@.planning/phases/01-package-structure/01-01-SUMMARY.md

<interfaces>
<!-- Verified patterns from 01-RESEARCH.md — use exactly as shown -->

pyproject.toml [project.scripts] — add pluginlint:
```toml
[project.scripts]
skilllint  = "skilllint.plugin_validator:app"
agentlint  = "skilllint.plugin_validator:app"
pluginlint = "skilllint.plugin_validator:app"
skillint   = "skilllint.plugin_validator:app"
```

pyproject.toml wheel exclusion (add to existing [tool.hatch.build.targets.wheel]):
```toml
[tool.hatch.build.targets.wheel]
include = ["packages/skilllint"]
exclude = ["packages/skilllint/tests"]
```

importlib.resources access pattern (Python 3.11+):
```python
from importlib.resources import files
import json

def load_bundled_schema(platform: str, version: str = "v1") -> dict:
    ref = files(f"skilllint.schemas.{platform}").joinpath(f"{version}.json")
    return json.loads(ref.read_bytes())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pluginlint entry point and exclude tests from wheel</name>
  <files>pyproject.toml</files>
  <action>
    Edit pyproject.toml to make two targeted changes:

    1. **Add pluginlint to [project.scripts]**: The current [project.scripts] section has 3 entries (skilllint, agentlint, skillint). Add the missing fourth:
       ```toml
       pluginlint = "skilllint.plugin_validator:app"
       ```
       All four entries must point to exactly `"skilllint.plugin_validator:app"`.

    2. **Exclude tests from wheel**: The current [tool.hatch.build.targets.wheel] section only has `include = ["packages/skilllint"]`. Add:
       ```toml
       exclude = ["packages/skilllint/tests"]
       ```

    After editing, run `uv build` to produce a new wheel, then verify:
    - `unzip -l dist/skilllint-*.whl | grep pluginlint` — should show the pluginlint script
    - `unzip -l dist/skilllint-*.whl | grep tests` — should return NO results (tests excluded)
    - Install into a temp tool env and test all four aliases:
      ```bash
      uv tool install --force dist/skilllint-*.whl
      skilllint --help
      agentlint --help
      pluginlint --help
      skillint --help
      ```
      All four must exit 0 and produce identical output.
  </action>
  <verify>
    <automated>cd /home/ubuntulinuxqa2/repos/agentskills-linter && uv build --quiet && unzip -l dist/skilllint-*.whl | grep -E "(pluginlint|tests)" && uv tool install --force dist/skilllint-*.whl --quiet && pluginlint --help | head -3</automated>
  </verify>
  <done>
    - pyproject.toml [project.scripts] has all 4 entries including pluginlint
    - uv build succeeds
    - `unzip -l dist/*.whl | grep tests` returns empty (tests excluded from wheel)
    - `pluginlint --help` exits 0 and matches `skilllint --help` output
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bundled schema directory and v1.json</name>
  <files>
    packages/skilllint/schemas/__init__.py
    packages/skilllint/schemas/claude_code/__init__.py
    packages/skilllint/schemas/claude_code/v1.json
  </files>
  <behavior>
    - `importlib.resources.files("skilllint.schemas.claude_code").joinpath("v1.json").read_bytes()` returns valid JSON bytes
    - `json.loads(...)` on those bytes succeeds and returns a dict with key "$schema"
    - The wheel produced by `uv build` contains `skilllint/schemas/claude_code/v1.json`
    - `load_bundled_schema("claude_code")` returns a dict (function to be added to __init__.py or a new schemas.py utility)
  </behavior>
  <action>
    Create the schema directory structure inside packages/skilllint/:

    1. Create `packages/skilllint/schemas/__init__.py` — empty file (aids IDE navigation; importlib.resources.files() works without it on Python 3.11+ but include for clarity)

    2. Create `packages/skilllint/schemas/claude_code/__init__.py` — empty file

    3. Create `packages/skilllint/schemas/claude_code/v1.json` — minimal JSON Schema for Claude Code platform. This is a placeholder that Phase 2 will fill in with the PlatformAdapter-driven schema. For Phase 1, it must be valid JSON and capture the field names the existing Pydantic models (SkillFrontmatter, AgentFrontmatter, CommandFrontmatter in frontmatter_core.py) validate:

    ```json
    {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "skilllint/schemas/claude_code/v1.json",
      "title": "Claude Code Platform Schema v1",
      "description": "Bundled schema snapshot for Claude Code plugins, skills, agents, and commands. Accessed via importlib.resources.files() at runtime. Full adapter-driven schema loading added in Phase 2.",
      "version": "1.0.0",
      "platform": "claude_code",
      "file_types": {
        "skill": {
          "required_fields": ["name", "version", "description"],
          "optional_fields": ["author", "triggers", "tools", "permissions", "dependencies"]
        },
        "agent": {
          "required_fields": ["name", "version", "description"],
          "optional_fields": ["author", "tools", "permissions", "model"]
        },
        "command": {
          "required_fields": ["name", "version"],
          "optional_fields": ["description", "author"]
        },
        "plugin": {
          "required_fields": ["name", "version", "description"],
          "optional_fields": ["author", "skills", "agents", "commands", "hooks"]
        }
      }
    }
    ```

    4. Add a `load_bundled_schema()` utility function to `packages/skilllint/__init__.py` so it is accessible as `from skilllint import load_bundled_schema`:

    ```python
    from __future__ import annotations

    import json
    from importlib.resources import files

    from skilllint.version import __version__

    __all__ = ["__version__", "load_bundled_schema"]


    def load_bundled_schema(platform: str, version: str = "v1") -> dict:
        """Load a bundled platform schema snapshot.

        Args:
            platform: Platform identifier, e.g. 'claude_code'
            version: Schema version, defaults to 'v1'

        Returns:
            Parsed JSON schema as a dict
        """
        ref = files(f"skilllint.schemas.{platform}").joinpath(f"{version}.json")
        return json.loads(ref.read_bytes())
    ```

    5. Rebuild the wheel: `uv build`

    6. Verify schema is in the wheel:
       `unzip -l dist/skilllint-*.whl | grep schemas`
       Must show `skilllint/schemas/claude_code/v1.json`

    7. Verify importlib access works:
       `python -c "from skilllint import load_bundled_schema; s = load_bundled_schema('claude_code'); print(s['platform'])"`
       Must print `claude_code`
  </action>
  <verify>
    <automated>cd /home/ubuntulinuxqa2/repos/agentskills-linter && uv build --quiet && unzip -l dist/skilllint-*.whl | grep "schemas/claude_code/v1.json" && python -c "from skilllint import load_bundled_schema; s = load_bundled_schema('claude_code'); assert s['platform'] == 'claude_code'; print('schema OK')"</automated>
  </verify>
  <done>
    - packages/skilllint/schemas/claude_code/v1.json exists and contains valid JSON with "$schema" and "platform" keys
    - `uv build` wheel contains `skilllint/schemas/claude_code/v1.json`
    - `load_bundled_schema("claude_code")` returns a dict with key "platform" == "claude_code"
    - `uv run pytest -q` still exits 0 (no regressions from __init__.py changes)
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cat pyproject.toml | grep pluginlint` — shows the entry
2. `uv build --quiet && unzip -l dist/skilllint-*.whl | grep tests` — empty output (no tests in wheel)
3. `unzip -l dist/skilllint-*.whl | grep "schemas/claude_code/v1.json"` — shows the file
4. `python -c "from skilllint import load_bundled_schema; print(load_bundled_schema('claude_code')['platform'])"` — prints `claude_code`
5. `uv run pytest -q 2>&1 | tail -5` — 521+ passing, 0 failing
</verification>

<success_criteria>
- All 4 CLI entry points defined in pyproject.toml and all 4 commands work after install
- tests/ excluded from wheel
- schemas/claude_code/v1.json bundled in wheel and accessible via importlib.resources.files()
- load_bundled_schema() function exported from skilllint package
- All tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-structure/01-02-SUMMARY.md` with:
- Confirmed entry points (paste [project.scripts] section)
- Schema file path and key fields
- Wheel contents verification output
- Test count (pass/skip/fail)
</output>
